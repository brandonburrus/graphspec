{
  "name": "graph-spec-cli",
  "version": "0.3.0",
  "description": "GraphSpec: a CLI + library for spec-driven development: author software specs as an OKF knowledge graph and build software by traversing it.",
  "type": "module",
  "license": "MIT",
  "author": "brandonburrus",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/brandonburrus/graphspec.git"
  },
  "homepage": "https://github.com/brandonburrus/graphspec#readme",
  "bugs": {
    "url": "https://github.com/brandonburrus/graphspec/issues"
  },
  "keywords": ["spec-driven-development", "okf", "knowledge-graph", "cli", "ai-agents"],
  "bin": {
    "graphspec": "dist/cli.js",
    "graph-spec-cli": "dist/cli.js"
  },
  "files": ["dist", "src", "skills", "README.md", "LICENSE"],
  "publishConfig": {
    "access": "public"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "prepack": "npm run build",
    "prepublishOnly": "npm run lint && npm run typecheck && npm run test",
    "version": "biome check --write package.json && git add package.json",
    "build": "tsc -p tsconfig.json && npm run build:viewer",
    "build:viewer": "esbuild src/viewer/main.ts --bundle --minify --format=iife --target=es2022 --outdir=dist/viewer --entry-names=viewer",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.viewer.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "npm run build && playwright test",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write ."
  },
  "dependencies": {
    "chalk": "^6.0.0",
    "commander": "^12.1.0",
    "figlet": "^1.11.4",
    "gray-matter": "^4.0.3"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@playwright/test": "^1.62.1",
    "@types/d3-force": "^3.0.10",
    "@types/node": "^22.10.2",
    "d3-force": "^3.0.0",
    "esbuild": "^0.28.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
